apiVersion: v1
kind: Service
metadata:
  name: devops-service
  labels:
    app: k8-devops-project
spec:
  selector:
    app: k8-devops-project
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31200
