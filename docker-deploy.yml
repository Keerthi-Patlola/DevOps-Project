apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-deployment
  labels:
    app: tomcat
spec:
  replicas: 3
  selector:
    matchLabels:
      app: tomcat
  template:
    metadata:
      labels:
        app: tomcat
    spec:
      containers:
      - name: tomcat
        image: keerthi1987/docimg
        ports:
        - containerPort: 80
